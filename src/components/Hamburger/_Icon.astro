<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" stroke="currentColor">
    <path d="M 0 50 L 50 50" class="top left"/>
    <path d="M 50 50 L 100 50" class="top right"/>
    <path d="M 0 50 L 50 50" class="middle left"/>
    <path d="M 50 50 L 100 50" class="middle right"/>
    <path d="M 0 50 L 50 50" class="bottom left"/>
    <path d="M 50 50 L 100 50" class="bottom right"/>
  </svg>

  <style>

svg{
      position: relative;
      width: 2rem;
      height: 3rem;
      stroke-width: 7;

  }

  .line {
    position: absolute;
    left: 0;
    top: clac(50% - 1px);
    width: 100%;
  }
  
  path{
    transition: transform .3s ease-in;
    transform-origin: center center;
  }

  .top {
    transform: translateY(-22%);
  }
  
  .bottom {
    transform: translateY(22%);
  }
  
  .middle {
    transform: translateX(0);;
    transition: transform 0.3s;
  }
  
  :global([aria-expanded="true"]) .middle.left {
    transform: translateX(-50%);
  }
  
  :global([aria-expanded="true"]) .middle.right {
    transform: translateX(50%);
  }


  :global([aria-expanded="true"]) .top.left{
    transform: rotate(45deg);
  }

  :global([aria-expanded="true"]) .top.right{
    transform: rotate(-45deg);
  }
  
  :global([aria-expanded="true"]) .bottom.left{
    transform: rotate(-45deg);
  }
  
  :global([aria-expanded="true"]) .bottom.right{
    transform: rotate(45deg);
  }
  </style>